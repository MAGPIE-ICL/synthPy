[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "synthPy"
authors = [{name = "Sam MacKay", email = "sam.mackay_i@outlook.com"}]
dynamic = ["1.0", "description"]
